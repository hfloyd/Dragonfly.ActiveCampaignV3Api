
namespace Dragonfly.ActiveCampaign.Models
{
    public enum ActiverssInterval { Day1 };

    public enum LastStep { Designer, Result, Summary };

    public enum ReminderField { Sdate };

    public enum ReminderFormat { Empty, YyyyMmDd };

    public enum ReminderOffsetSign { Empty };

    public enum OffsetType { Day };

    public enum ReminderType { MonthDay };

    public enum ResponderType { Subscribe };
    
    public enum Source { Copy, Web };

    public enum SplitType { Even };

    public enum TrackLinks { All, Mime, None };

    public enum TypeEnum { Single };
}
